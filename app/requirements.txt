# Versiones CPU, alineadas con el requirements.txt del TFM (torch==2.1.2,
# torch_geometric==2.3.1, torch_geometric_temporal==0.54.0) pero sin CUDA.
# torch y las extensiones de PyG se instalan aparte en el Dockerfile desde
# sus índices de wheels CPU (no están en PyPI en esa combinación).
numpy==1.26.4
pandas==1.3.5
fastapi==0.115.0
uvicorn[standard]==0.30.6
